export interface TellATaleSettings {
  characterTag: string;
  showAvatars: boolean;
}

export const DEFAULT_SETTINGS: TellATaleSettings = {
  characterTag: "dialogue-character",
  showAvatars: true,
};
